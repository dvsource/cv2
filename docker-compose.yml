services:
  cv-editor:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./cv.json:/app/cv.json
      - cv-db:/app/db-data
    env_file:
      - path: .env
        required: false
    environment:
      - CV_DB_PATH=/app/db-data/cv_history.db

volumes:
  cv-db:
